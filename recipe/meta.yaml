{% set name = "av" %}
{% set version = "0.5.3" %}
{% set sha256 = "198719246335103e8c16a50c1f269359b4d52c7c38586f39629d0b869c5bb64c" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script: python setup.py install --single-version-externally-managed --record record.txt

requirements:
  build:
    - python
    - setuptools
    - cython
    - pkg-config  # [not win]
    - msinttypes  # [win and py27]
    - ffmpeg 3.2.*
  run:
    - python
    - ffmpeg 3.2.*
    - pillow
    - numpy

test:
  imports:
    - av

about:
  home: https://github.com/mikeboers/PyAV/
  license: BSD 3-Clause
  license_file: LICENSE.txt
  summary: 'Pythonic bindings for FFmpeg.'

  description: |
    PyAV is a Pythonic binding for FFmpeg or Libav. We aim to provide all of
    the power and control of the underlying library, but manage the gritty
    details as much as possible.
  doc_url: http://mikeboers.github.io/PyAV/
  dev_url: https://github.com/mikeboers/PyAV/

extra:
  recipe-maintainers:
    - danielballan
    - caspervdw
    - mikeboers
